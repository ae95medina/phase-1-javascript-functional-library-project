function myEach(testArr, alert){
  const values = Object.values(testArr)
  values.forEach(value => alert(value))

return testArr
}
function myMap(testArr, callback){
const newArray = []
const values = Object.values(testArr).map(testArr => {
    return newArray
})
console.log(values, newArray)
}